local _ = require("gettext")

return {
    name = "pomodoro_total",
    fullname = _("Pomodoro (with total study time)"),
    description = _([[A Pomodoro focus/break timer that also keeps a running total of time studied, both for today and all-time. Access it from the main menu under "More tools".]]),
}
